name: Rust CI (deprecated)

# This workflow was consolidated into .github/workflows/ci.yml.
# Keeping a no-op workflow (manual dispatch) to avoid accidental triggers.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is deprecated; use .github/workflows/ci.yml"
