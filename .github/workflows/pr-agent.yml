name: PR Agent

# General-purpose LLM PR review (auto-describe + auto-review).
#
# The review step runs only when a provider API-key secret is configured
# (GEMINI_API_KEY / MISTRAL_API_KEY / CEREBRAS_API_KEY); with none present it
# skips cleanly. It sends the PR diff and metadata to an external LLM — Gemini
# primary, Mistral then Cerebras as litellm fallbacks — and posts a description
# and review. Advisory only (continue-on-error), so a provider outage or
# rate-limit never turns this into a red, blocking check. Remove the secrets to
# turn it off.

on:
  pull_request:
    types: [opened, reopened, ready_for_review]
  issue_comment:

permissions:
  issues: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.event.issue.number }}
  cancel-in-progress: true

jobs:
  pr-agent:
    # Skip bot senders and fork PRs (fork PRs never see secrets on a plain
    # pull_request trigger — GitHub redacts them). `!= true` (not `== false`)
    # because issue_comment events leave pull_request.head.repo.fork null, and
    # this job must still run for that event.
    if: ${{ github.event.sender.type != 'Bot' && github.event.pull_request.head.repo.fork != true }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # The agent step runs only when a provider key is configured; with none
      # present it skips cleanly and posts a notice. Remove the secrets to
      # disable the review entirely.
      - name: Check for a provider key
        id: key
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
        run: |
          if [ -n "$GEMINI_API_KEY" ] || [ -n "$MISTRAL_API_KEY" ] || [ -n "$CEREBRAS_API_KEY" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "::notice::PR Agent skipped — no provider API key configured (GEMINI_API_KEY / MISTRAL_API_KEY / CEREBRAS_API_KEY)."
          fi

      # Digest-pinned image so the review can't silently change behavior on a
      # tag re-push.
      # continue-on-error: advisory only — a provider outage, rate-limit, or bad
      # response must never turn this into a red, blocking check.
      - name: PR Agent action step
        if: steps.key.outputs.present == 'true'
        continue-on-error: true
        uses: docker://pragent/pr-agent@sha256:ea2ea90f072fd97708755e59827a317272f66097a1ef349eca23d39160bb0baf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Model IDs on every provider are point-in-time; expect to bump these
          # as providers retire models. Gemini primary, Mistral then Cerebras as
          # litellm fallbacks so a single provider's rate limit is not a SPOF.
          config.model: "gemini/gemini-3.5-flash"
          config.fallback_models: '["mistral/mistral-small-2603", "cerebras/gpt-oss-120b"]'
          # The fallback models are not in PR-Agent's built-in MAX_TOKENS table,
          # so without this they fail with "not defined in MAX_TOKENS" the moment
          # Gemini is unavailable (e.g. a 503), leaving the review silently
          # unposted. A positive custom_model_max_tokens gives any unknown model
          # a context budget so the fallback chain actually works. Both fallbacks
          # support >=128k context.
          config.custom_model_max_tokens: "128000"
          GOOGLE_AI_STUDIO.GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # PR-Agent maps MISTRAL.KEY to MISTRAL_API_KEY; Cerebras has no
          # PR-Agent-specific key, litellm reads CEREBRAS_API_KEY from the env.
          MISTRAL.KEY: ${{ secrets.MISTRAL_API_KEY }}
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          # describe + review only. auto_improve must be an explicit "false" — if
          # left unset, PR-Agent still runs /improve.
          github_action_config.auto_describe: "true"
          github_action_config.auto_review: "true"
          github_action_config.auto_improve: "false"
