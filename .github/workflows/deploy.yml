# Deprecated deploy workflow (Deno) — disabled
# This workflow was for a Deno-based deployment pipeline which is no longer
# applicable to this Rust repository. Keeping it as a disabled workflow for
# reference; remove if you no longer need it.

name: Deploy (disabled)

on: {}

jobs: {}
